# Environment variables for WhatsApp MCP Server

# Server Configuration
PORT=8080
LOG_LEVEL=info

# Database Configuration
WHATSAPP_DB_PATH=./whatsapp.db

# Media Configuration
WHATSAPP_MEDIA_DIR=./media
QR_CODE_DIR=./qr_codes

# TTS Configuration
TTS_URL=http://localhost:8001/text-to-speech

# STT Configuration (Speech-to-Text)
# Set this to use an external STT service (e.g., voice-api-service in Kubernetes)
# If not set, will use local whisper or OpenAI API
STT_URL=http://voice-api-service/speech-to-text

# Optional: WhatsApp Configuration
# WHATSAPP_SESSION_NAME=default
# WHATSAPP_LOG_LEVEL=info

# LlamaStack Configuration
LLAMASTACK_BASE_URL=http://ragathon-team-1-ragathon-team-1.apps.llama-rag-pool-b84hp.aws.rh-ods.com/
LLAMASTACK_API_KEY=your_api_key_here
LLAMASTACK_MODEL=vllm-inference/llama-4-scout-17b-16e-w4a16
LLAMASTACK_TEMPERATURE=0.7
LLAMASTACK_MAX_TOKENS=200

# Vector Store Configuration
# VECTOR_STORE_NAME is used to look up the vector store ID by name
# Default: redbank-kb-vector-store
VECTOR_STORE_NAME=redbank-kb-vector-store

# MCP Server Configuration
# MCP_URL is used to configure the MCP server directly in the agent tools
# Default: http://redbank-mcp-server:8000/mcp
MCP_URL=http://redbank-mcp-server:8000/mcp

# MCP Server Label
# MCP_SERVER_LABEL is used as the server_label in the MCP tool configuration
# Default: dmcp
MCP_SERVER_LABEL=dmcp

# Banking Assistant Configuration
# The agent will use MCP tools and knowledge search for banking operations

# MCP Tool Group Configuration (optional fallback)
# If MCP_URL is not set, you can use a pre-registered tool group name instead
# Use 'client.toolgroups.list()' or check logs to see available tool groups
# LLAMASTACK_MCP_TOOL_GROUP=mcp::redbank-financials
