apiVersion: v1
kind: Service
metadata:
  name: whatsapp-mcp-server
  labels:
    app: whatsapp-mcp-server
spec:
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: whatsapp-mcp-server
  type: ClusterIP
